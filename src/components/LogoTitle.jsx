import {Image} from 'react-native';
import logo from '../images/mechano.png';

const LogoTitle = () => {
  return <Image style={{width: 140, height: 30}} source={logo} />;
};

export default LogoTitle;
